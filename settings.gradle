rootProject.name = 'e-commerce'

// 三个模块。order-service 与 inventory-service 之间【故意】没有依赖关系，
// 它们只能通过 HTTP / Kafka 通信 —— 这在编译期就强制了「数据所有权」铁律。
include 'common'
include 'order-service'
include 'inventory-service'
